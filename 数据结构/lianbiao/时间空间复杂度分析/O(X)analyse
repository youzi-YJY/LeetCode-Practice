复杂度权衡：时间、空间
如下给出链表相关操作的时间和空间复杂度分析：
1.在第i个位置插入，平均O(n)
2.在第i个位置替换，平均O(n)
3.在开始处插入，最好和最差情况O(1)
4.从开始处删除，最好和最差情况O(1)
5.在第i个位置插入，平均情况O(n)
6.从第i个位置删除，平均情况O(n)

单链表的结构相对于数组的主要优点在于内存性能，而不是时间性能。当必须调整数组的大小时，
其时间和内存都是线性的。

链表确实有一个额外的浪费内存的问题，单链表必须要为指针使用n个内存单元格，这在双链表的结构中代价会
翻倍。
